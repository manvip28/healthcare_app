name: healthcare_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  sensors_plus: ^4.0.2
  tflite_flutter: ^0.11.0
  shared_preferences: ^2.2.2
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  intl: ^0.19.0
  permission_handler: ^11.0.1
  fl_chart: ^0.65.0
  geolocator: ^10.1.0
  cupertino_icons: ^1.0.2
  file_picker: ^5.3.1
  pdf: ^3.6.0
  printing: ^5.13.4
  flutter_local_notifications: ^16.3.0
  timezone: ^0.9.2
  open_file: ^3.0.0
  url_launcher: ^6.0.9
  flutter_localizations:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.6
  intl_utils: ^2.5.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/models/diabetes_trend_model.tflite

